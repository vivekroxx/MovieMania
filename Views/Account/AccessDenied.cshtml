@model LoginViewModel

@{
    Layout = "_Layout.Unauthenticated";
    ViewData["Title"] = "Access Denied";
}

<div class="card">
    <div class="card-body">
        <h1 class="text-center text-danger">Access Denied!</h1>
        <p>Sorry you cannot access this page</p>
        <a class="btn btn-primary" asp-action="Index" asp-controller="Home">Click here to go back</a>
    </div>
</div>
